{
  "buildCommand": "npm run build && cp -r dist/client/* dist/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/assets/(.*)", "destination": "/assets/$1" },
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
